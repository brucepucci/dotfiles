# Paths here are relative to the destination directory (~), and are left
# completely alone by chezmoi: never added, never applied, never removed.

# Repo furniture that belongs to the dotfiles repo, not to $HOME.
README.md

# Note: ~/.config/nvim/lazy-lock.json is deliberately NOT ignored. It pins every
# plugin to an exact commit, which is what makes a new machine reproduce this
# one. After `:Lazy update`, run `chezmoi re-add` to record the new versions.
