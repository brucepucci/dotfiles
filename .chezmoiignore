# Paths here are relative to the destination directory (~), and are left
# completely alone by chezmoi: never added, never applied, never removed.

# Generated by packer on every :PackerSync / :PackerCompile.
.config/nvim/plugin/packer_compiled.lua

# Repo furniture that belongs to the dotfiles repo, not to $HOME.
README.md
