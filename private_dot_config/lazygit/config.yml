# lazygit reads diffs through delta, matching what `git diff` shows in the
# shell. Neovim does not use this -- gitsigns and diffview render their own.
git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never
gui:
  showListFooter: false
  nerdFontsVersion: "3"
